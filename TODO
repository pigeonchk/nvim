* setup new keymaps (or just stick with the defaults) of the treesitter
* check new treesitter modules: https://github.com/nvim-treesitter/nvim-treesitter/wiki/Extra-modules-and-plugins
* Customize CoC
     -- Highlight the symbol and its references when holding the cursor
     autocmd {'CursorHold', '*', 'call CocActionAsync("highlight")'}
* customize vim-surround
* customize 'shada'
* make a system where it asks at startup whether to restore a session, if there's one.
  also, asks to save session on :wq!
* add a plugin that checks for all included file (:checkpath) and reports as a notification if
  the header was not found. Run at every :write?
* tags in neovim (:h tagsrch.txt)
* compiling in neovim (:h quickfix.txt)
